/**
 * Default brand colors used when hub/publication branding is not set.
 * Matches the design system primary (deep teal) and neutral (charcoal).
 */

export const DEFAULT_BRAND_HEX = '#1a6b6b';
export const DEFAULT_SECONDARY_HEX = '#2a2a2a';
